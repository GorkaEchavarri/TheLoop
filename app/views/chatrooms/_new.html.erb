<p>Create chat</p>
<%= form_with(model: chatroom, remote: true, class: "d-flex" ) do |f| %>
  <%= f.hidden_field :receiver_id, { value: user.id } %>
  <%= f.text_field :name, class: "form-control", autocomplete: 'off' %>
  <%= f.submit data: { disable_with: false } %>
<% end %>
